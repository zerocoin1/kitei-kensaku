"""アプリ全体の設定値。

会社名・保険種目・使用モデルを変えたいときは、基本的にこのファイルだけを
編集すれば済むようにしています。
"""
from pathlib import Path

# ---- パス（原則そのままでOK）----
BASE_DIR = Path(__file__).resolve().parent.parent
DATA_DIR = BASE_DIR / "data"
PDF_DIR = DATA_DIR / "pdfs"          # ローカル/ダウンロードしたPDFの置き場
INDEX_DIR = DATA_DIR / "index"       # ベクトルインデックスの保存先
STORE_PATH = INDEX_DIR / "store.pkl"  # ベクトル＋メタ情報の本体
MANIFEST_PATH = DATA_DIR / "manifest.json"  # 取り込み済みファイルの記録
# Driveに保存する「構築済み索引」ファイル名。起動時にこれを読み込めば再構築が不要になる。
INDEX_FILE_NAME = "index_store.pkl"

# ---- データソース ----
# "drive": Google Driveから自動同期する
# "local": data/pdfs フォルダを直接読む（Googleドライブ デスクトップ同期などと併用）
SOURCE_MODE = "drive"

# ---- 分類（画面プルダウンの選択肢）----
# ★ここを実際の8社・実際の種目に書き換えてください。
#   Google Drive側のフォルダ名と完全一致させること（例: /保険規定データ/損保ジャパン/自動車保険/）
COMPANIES = [
    "損保ジャパン",
    "B損害保険", "C損害保険", "D損害保険",
    "E損害保険", "F損害保険", "G損害保険", "H損害保険",
]
PRODUCT_TYPES = [
    "自動車保険", "火災保険", "傷害", "賠償",
    "新種", "海上保険", "その他",
]

# ---- 商品（種目の中の細分類。ファイル名→中身の順で自動判定）----
# 「すべて」を選ぶと種目内を横断検索。特定商品を選ぶとその商品だけに絞る。
# 約款も取扱規定集も、同じ商品ライン（BAP/THEクルマ・SGP/ドライバー）にまとめる。
# (表示名, ファイル名に含まれるキーワードのリスト) の順。上から最初に一致したものを採用。
PRODUCT_RULES = [
    ("BAP", ["BAP"]),
    ("THEクルマ・SGP", ["THEクルマ", "ＴＨＥクルマ", "SGP", "ＳＧＰ"]),
    ("ドライバー保険", ["ドライバー"]),
]
# ファイル名で商品ラインが分からない場合（例：「損保ジャパン自動車保険約款.pdf」）、
# PDF先頭ページの本文に含まれる語で判定する。上から最初に一致したものを採用。
PRODUCT_CONTENT_RULES = [
    ("THEクルマ・SGP", ["クルマの保険", "個人用自動車保険"]),
    ("BAP", ["一般自動車保険"]),
    ("ドライバー保険", ["ドライバー保険"]),
]
PRODUCT_ANY = "すべて"        # 商品で絞らないときの選択肢
PRODUCT_OTHER = "その他"      # どのルールにも一致しなかったファイルの商品名

# ---- モデル設定 ----
# 生成モデル。新しいモデルが出たらここを差し替えるだけでOK。
#   "gemini-2.5-flash"        … 安定版。信頼性が高く安価（本番採用・推奨）
#   "gemini-3.5-flash"        … 最新・高品質だが高価（同等品質なら2.5で十分）
#   "gemini-3-flash-preview"  … 試験版。橋渡しは賢いが不安定（詰まることあり）
LLM_MODEL = "gemini-2.5-flash"
# 埋め込み（ベクトル化）モデル。※text-embedding-004は現行APIで廃止。
#   "gemini-embedding-001" … 現行の安定版（実証済み）
EMBED_MODEL = "gemini-embedding-001"
EMBED_DIM = 768              # 埋め込みベクトルの次元（768で十分・軽量）

# ---- 検索チューニング ----
TOP_K = 6                    # 検索で拾う関連ページ数
PAGE_CHAR_LIMIT = 1800       # 1ページからLLMに渡す最大文字数（長すぎるページの切り詰め）
MIN_PAGE_CHARS = 50          # これ未満の文字数のページは索引に入れない（白紙・画像ページ除外）
